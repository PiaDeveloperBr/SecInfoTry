<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppThemeNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Progress dialog theme-->
    <style name="ProgressDialogTheme" parent="Base.Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!--Edittext style-->
    <style name="DingoEditText" parent="Base.Widget.AppCompat.EditText">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">@dimen/size_edittexts</item>
        <item name="android:textColorHint">@color/hint_edittexts</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/appAlphaBlack</item>
        <item name="android:backgroundTint" tools:ignore="NewApi">@color/appAlphaBlack</item>
    </style>

    <!--Button style-->
    <style name="DingoButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/ripple_button</item>
        <item name="android:textColor">@color/textButtonWhite</item>
        <item name="android:textSize">17dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
